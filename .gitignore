_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
vendor/
.DS_Store

# Video files (host on Cloudflare R2 instead)
*.mp4
*.mov
*.avi
*.webm

# All images served from Cloudflare R2
assets/**/*.jpg
assets/**/*.jpeg
assets/**/*.png
assets/**/*.gif
assets/**/*.webp
assets/**/*.pdf

# Source Word documents (not needed for Jekyll)
*.docx
Modules*/
test.srt

# Wrangler / Cloudflare local state
.wrangler/

# Local a11y testing tooling (not part of the site; kept locally, not committed)
node_modules/
package.json
package-lock.json
a11y-repro.mjs
a11y-axe.mjs

# Local environment variables (API keys — do not commit)
.env

# Working files not part of the site
*.numbers
/HOW_TO.md

# Subtitle project files
*.ass
*.atpj

# Audio files served from Cloudflare R2
*.mp3

# Generated subtitle working files
assets/subtitles/*_soundlabels.vtt
assets/subtitles/*_audiodesc_audio/

# local video working copies (uploaded to R2, never committed)
clips_out/
video_sources.tsv
